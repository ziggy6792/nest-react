# Database URL for SQLite (LibSQL)
# For local development, use a file-based SQLite database
DATABASE_URL=file:./db.sqlite

# Optional: Server port (defaults to 3001)
PORT=3001

# Optional: Node environment
NODE_ENV=development
